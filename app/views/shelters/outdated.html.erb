<%= content_for :page { render @page } %>

<%
extra_commands = []
if user_signed_in?
  extra_commands += [
    {
      label: 'Mark Current', path: ->(record) { [:mark_current, record] },
      args: { method: :post, class: 'button button-clear' }
    }
  ]
end
%>
<%= render 'shared/table', rows: @shelters,
                           headers: @headers,
                           columns: @columns,
                           commands: table_command_list(:archive, extra_commands),
                           reverse_chron: true %>
