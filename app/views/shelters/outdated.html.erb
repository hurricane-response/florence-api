<h1>Shelters Needing Updates</h2>

<%= render 'shared/table', rows: @shelters,
                           headers: @headers,
                           columns: @columns,
                           commands: table_command_list(:archive),
                           reverse_chron: true %>
